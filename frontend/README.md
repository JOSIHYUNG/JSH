# Frontend

Vite, React, TypeScript 기반 프론트엔드입니다. 개발 서버의 `/api` 요청은 `http://127.0.0.1:8000` 백엔드로 프록시됩니다.

## 실행

```powershell
npm install
npm run dev
```

기본 주소: <http://localhost:5173>

## 검증

```powershell
npm run build
npm run lint
```
